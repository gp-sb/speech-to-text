# Speech-to-Text — Python Dependencies
# =====================================

# Core STT engine (Apple Silicon optimized)
parakeet-mlx>=0.5.0

# Audio capture
sounddevice>=0.4.6
soundfile>=0.12.0

# Audio array handling
numpy>=1.24.0

# Global hotkey detection
pynput>=1.7.6

# macOS menu bar app
rumps>=0.4.0

# Configuration
pyyaml>=6.0

# Fallback STT engine (uncomment if parakeet-mlx doesn't work)
# faster-whisper>=1.0.0
